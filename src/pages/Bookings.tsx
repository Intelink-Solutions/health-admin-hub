import { AdminLayout } from "@/components/layout/AdminLayout";
import { BookingsPage } from "@/components/ecommerce/BookingsPage";

const Bookings = () => (
  <AdminLayout>
    <BookingsPage />
  </AdminLayout>
);

export default Bookings;
